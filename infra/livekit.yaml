port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: false
  # Container's internal IP isn't reachable from the host/browser under Docker Desktop;
  # advertise loopback so ICE candidates resolve for same-machine dev/testing.
  node_ip: 127.0.0.1
keys:
  devkey: devsecretdevsecretdevsecretdevsecret